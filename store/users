create database store;
create table users (
    id int primary key,
    full_name   varchar(20),
    email      varchar(20) unique,
    gender char(25),
    date_of_birth varchar(15),
    created_at datetime,
    country_cods int ,
);